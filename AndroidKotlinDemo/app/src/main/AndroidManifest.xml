<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.yueban.androidkotlindemo">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.lifecycle.LifecycleActivity1"/>
        <activity android:name=".ui.lifecycle.LifecycleActivity2"/>
        <activity android:name=".ui.livedata.LiveDataActivity1"/>
        <activity android:name=".ui.room.RoomActivity1"/>
        <activity android:name=".ui.viewmodel.ViewModelActivity1"/>
        <activity android:name=".ui.viewmodel.ViewModelActivity2"/>
        <!-- room with a view demo -->
        <activity
                android:name=".demo.roomwithview.ui.RoomWithViewActivity"
                android:label="RoomWithViewActivity"
                android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".demo.roomwithview.ui.NewWordActivity"/>
    </application>

</manifest>